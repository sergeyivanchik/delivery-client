import styled from 'styled-components';

import { Loading } from '@/components';

const LoadingStyled = styled(Loading)`
  display: block;
  margin: 50px auto;
`;

const SliderStyled = styled.div`
  width: 100%;
`;

export { LoadingStyled, SliderStyled };
